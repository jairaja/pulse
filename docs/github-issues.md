# Suggested GitHub Issues

1. Bootstrap Expo Router + TypeScript foundation.
2. Implement drawer navigation (Home, History, My Pulse, Submit Question, Settings, About).
3. Build prediction-first vote flow on Home.
4. Enforce prediction submission before voting.
5. Integrate Supabase client and typed API layer.
6. Create SQL schema migration for core tables.
7. Add unique vote constraint `(device_id, question_id)`.
8. Implement RPC for live world/country result aggregation.
9. Implement world heatmap data RPC.
10. Build real-time result polling with React Query.
11. Add Victory chart result visualization.
12. Render neon-glow world heatmap with react-native-maps.
13. Create share card and share channel UX.
14. Add link-to-website share message copy.
15. Implement anonymous identity bootstrapping (UUID + device ID).
16. Add IP hash rate-limiting in Edge Function.
17. Build History screen with answered-only query restriction.
18. Build My Pulse accuracy + agreement metrics (min 10 answers).
19. Create question submission flow (1 per week limit).
20. Add submission vote-up flow (1 per day rule).
21. Build automatic daily winner selection job.
22. Build moderator/developer control endpoints.
23. Add moderation states (abusive, duplicate, veto, disabled).
24. Integrate translation ingestion (Google + OpenAI).
25. Add translation quality evaluator and selection.
26. Add original-language toggle in UI.
27. Implement Settings persistence (theme, country, language).
28. Add offline connectivity monitoring and fallback UX.
29. Configure FCM and token registration.
30. Schedule 12:00 GMT daily push.
31. Implement About screen privacy-first copy.
32. Add telemetry/observability (Sentry + structured logs).
33. Add CI for lint, typecheck, unit tests, migration checks.
34. Create EAS build + deployment pipelines.
